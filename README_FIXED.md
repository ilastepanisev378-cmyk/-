# Сейко — Пузатый FIXED

Это исправленная версия присланного проекта.

Что изменено:
- аудио больше не запускается до полной инициализации интерфейса;
- добавлена безопасная обработка ошибок аудио;
- Kivy-конфигурация задаётся до создания окна;
- Kivy 2.3.1 используется с официальной поддержкой Python 3.13;
- запускной BAT сам проверяет Kivy и при необходимости устанавливает зависимости;
- добавлен `check_install.bat`;
- добавлен `run_coffee_debug.bat` с сохранением лога;
- сохранены исходные изображения, музыка, шрифты и игровая логика.

Запуск:
1. Распаковать ZIP.
2. Запустить `run_coffee.bat`.
3. Если проблема останется — запустить `run_coffee_debug.bat` и посмотреть `seiko_error.log`.

Проверка исходников в этой сборке:
- `main.py` и `managers.py` проходят Python syntax compilation;
- все PNG/JPG/GIF из assets проходят проверку изображения;
- все ссылки на используемые игровые assets присутствуют.
